import math


dividend = float(input("Enter the dividend: ")) #except ValueError
divisor = float(input("Enter the divisor: ")) #except ValueError:
quotient = dividend/divisor #devide by zero
print(math.floor(quotient)) 